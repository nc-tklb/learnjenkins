task('hello') {
    doLast {
        println "HELLO!"
    }
}
task('createLdapUser', description: 'Test function for create ldap user in actual system'){
    String _username = requiredParameter("user")
    doLast {
        println _username
    }
}
