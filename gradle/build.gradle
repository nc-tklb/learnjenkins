
task('hello') {
    doLast {
        println "HELLO!"
    }
}
task('createLdapUser', description: 'Test function for create ldap user in actual system'){
    String _username = "UNCHANGED"
    if(project.hasProperty('username')){
        _username=project.property('username')
    }
    doLast {
        println _username
    }
}
